<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>
<p class="text-center"><i>
  Registration is intended for the parent volunteer that is running the giftcard campaign. <br>
  Not sure how this works? take the <%= link_to "tour", tour_path %> and then decide if you want to sign up.
</i></p>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create my account", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
